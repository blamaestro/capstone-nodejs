import express from 'express';

const router = express.Router();

router.get('/', () => {});

router.get('/:id/logs', () => {});

router.post('/', () => {});

router.post('/:id/exercises', () => {});

export default router;
